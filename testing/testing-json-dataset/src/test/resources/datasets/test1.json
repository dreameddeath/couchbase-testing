#$dataset_name("test1")
#$import("subDataset1")

#%% Integer x,y,z; x=1;y=2;log.debug(x+y);z=x+y;
#%
        log.debug(x);
        log.debug(y);
        z+y+x;
%#

#$dataset_elt_name("the first dataset")
{
  "a new test is borned":[
      {
              "toto": @Test "tutut",
              tata[0..2].tutu : [ "toto", "v2", "v3"]
      }
  ]
}